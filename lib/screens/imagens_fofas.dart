import 'package:flutter/material.dart';

class ImagensFofas extends StatelessWidget {
  @override
  Widget build(context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.green[100],
      ),
    );
  }
}